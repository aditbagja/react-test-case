import { Typography } from "antd";

const Footer = () => {
  const { Title } = Typography;
  return (
    <footer>
      <Title level={5}>&copy; : 2023 | aditbagja</Title>
    </footer>
  );
};

export default Footer;
